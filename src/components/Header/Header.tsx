import React from 'react';

interface Props {}

const Header: React.FC<Props> = () => {

    return (
        <div className="timdb-header">
            HEADER BODY
        </div>
    );
};

export default Header;